//it is a package manager for external modules
//node package manager
//bits of reusable code that somone else wrote which we can use it

//npm init (it gives package.json)
//then install any external package by using npm install <packageName>//
//which gets added into our dependency
//it depends on it because we are requiring it inside our project