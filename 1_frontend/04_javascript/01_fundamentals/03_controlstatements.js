// === is equal to
// !== is not equal to
// > is greater than
// < is lesser than
// >= is greater than or equal to
// <= is lesser thanm or equal to
// == equality
// === checks if the data types are also matching

prompt("enter yor name");
prompt("enter your lovers name");

var loveScore = Math.floor(Math.random()*101);

if(loveScore === 100){ 
    alert("your love score is :" + loveScore + "%" + "you will get married");
}else{
    alert("your love score is :" + loveScore + "%" + " "+ "and it is great love");
}
